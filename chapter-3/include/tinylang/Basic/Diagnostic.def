#ifndef DIAG
#define DIAG(ID, Level, Msg)
#endif
DIAG(err_sym_declared, Error, "symbol {0} already declared")
#undef DIAG

// First param - enum label (how we refer to the error)
// Second param - severity
// Third level - message text

// NOTE - I believe the macro body is empty
// here because according to the book, it may
// be included several times "with different
// definitions for the macro."